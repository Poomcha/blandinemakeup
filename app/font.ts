import { Patrick_Hand_SC, Sono } from "next/font/google";

const patrick_hand_sc = Patrick_Hand_SC({
  weight: "400",
  subsets: ["latin"],
});

const sono = Sono({ subsets: ["latin"] });

export { patrick_hand_sc, sono };
